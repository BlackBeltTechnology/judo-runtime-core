# AGENTS.md — `judo-runtime-core-guice-testkit/src/main/java/hu/blackbelt/judo/runtime/core/guice/testkit`

| File | Purpose |
| --- | --- |
| `logback.xml` | Logback config: `CONSOLE` appender with `PatternLayout` pattern `%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n`; `root` logger at level `error` refs it. |