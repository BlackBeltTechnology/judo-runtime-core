# AGENTS.md — `judo-runtime-core-guice-testkit/src/test/java/hu/blackbelt/judo/runtime/core/guice/testkit/fixture`

| File | Purpose |
| --- | --- |
| `BackwardsCompatBuildModeTest.java` | Guards `JudoRuntimeFixture#init(Module, Object)` stays overridable: subclass override (`CountingFixture`) must fire on BY_METHOD path. Breaks if `init` made final/static; overrides never fire on cached BY_CLASS/SINGLETON path. |
| `CacheCloseableResourceTest.java` | Asserts `CachedRuntime#close()` idempotent: second close silent, null bundled artifacts safe, `closed` flag flips. Callers relying on exact-once close semantics break if double-close starts throwing. |
| `CacheKeyEqualityTest.java` | Asserts `ByClassCacheKey` distinguishes configs by `modules`/`interceptors`: equal annotations → equal keys, differing modules/interceptors → different keys, so cached runtimes never shared across such configs. |
| `ColdPathModelLoaderTest.java` | Regression guard: class-level `@JudoTest` reuses class-scoped preloaded model, method-level must NOT (exercises package-private `coldPathModelLoader(boolean, ExtensionContext.Store)`). Method-level inheriting class model fails test. |
| `CountingLiquibaseExecutorTest.java` | Verifies `CountingLiquibaseExecutor` counts only successful `createDatabase`: failed-then-retry must not inflate count. `StubCountingLiquibaseExecutor` overrides `createDatabase` to simulate success/failure. |
| `DefaultIsFreshRuntimePerMethodTest.java` | Asserts `@JudoTest#shareInjector()` defaults `false` → default class-level annotation cold-paths for `BY_CLASS` and `SINGLETON` (fresh runtime per method); `useCache` returns false at default. |
| `JudoRuntimeFixtureInterceptorTest.java` | Unit tests interceptor support in `JudoRuntimeFixture`: register by class/instance, null inputs, wrong timing, missing constructors, multiple interceptors, runtime modification. Exports `SimpleTestInterceptor`, `AnotherTestInterceptor`, `InterceptorWithoutNoArgConstructor`. `@Disabled` — requires real JUDO model `'exa'`. |
| `MethodLevelShareInjectorIgnoredTest.java` | Method-level `@JudoTest` always behaves as `BY_METHOD`: `shareInjector` flag ignored, `useCache` false regardless of flag value; class-scoped cache never activates for method-level annotations. |
| `PrepareWithCachedRuntimeTest.java` | Verifies `JudoRuntimeFixture#prepareWithCachedRuntime`: installs every `CachedRuntime` field, calls `injector.injectMembers` when `injectModulesTo` non-null, skips it when null, fails fast on null cached runtime. Export: `InjectionTarget` holder. |
| `ResolveDialectTest.java` | Unit tests `JudoRuntimeFixture#resolveDialect(String)`: `'hsqldb'` → `HsqldbDialect`, `'postgresql'` → `PostgresqlDialect`, unknown/null → `IllegalArgumentException` naming the dialect, each call returns fresh instance. |
| `RoutingPredicateTest.java` | Exhaustive tests of `JudoTestExtensionRouting`: `isClassScopedMode()` and full `useCache()` matrix; collapsed rule `useCache = isClassLevel && mode != BY_METHOD && shareInjector`, no per-mode carve-outs. |
| `ShareInjectorFlagTest.java` | Functional check of `@JudoTest#shareInjector`: boolean element default `false`, legacy `cacheRuntime` element removed, `JudoTestExtension#singletonRuntimesForTesting()` unpopulated when flag false. |
| `SingletonDatasourceKeyingTest.java` | JNG-6374 regression: SINGLETON datasource cache keyed by `(dialect, container)` — distinct keys must NOT share `JudoDatasourceFixture`, else schema mismatch. Operates via `singletonDatasourcesForTesting()`, clears via `closeAllSingletonRuntimes()`. |
| `SingletonModelLoaderKeyingTest.java` | JNG-6374 regression: SINGLETON model loader cache keyed by `(modelName, dialect, modelSource)` — distinct keys must NOT share `JudoModelLoader`. Operates via `singletonModelLoadersForTesting()`. |
| `SingletonRuntimeMapTest.java` | Singleton runtime map identity/lifecycle: equal `ByClassCacheKey` → same `CachedRuntime`, distinct keys → different; `closeAllSingletonRuntimes()` closes each entry once and clears map. |
| `SingletonShareInjectorFlagTest.java` | Routing coverage of `SINGLETON + shareInjector` combos: uniform with `BY_CLASS` (reverses the always-cache carve-out); method-level SINGLETON still bypasses cache. |